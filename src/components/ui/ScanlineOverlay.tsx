import { useEffect, useState } from "react";

const ScanlineOverlay = () => {
    return (
        <div className="glitch-lines-container">
            <div className="glitch-line" />
            <div className="glitch-line" />
            <div className="glitch-line" />
            <div className="glitch-line" />
        </div>
    );
};

export default ScanlineOverlay;
